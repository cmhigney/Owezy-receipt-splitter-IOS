rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Client SDK writes are blocked; server SDK in Cloud Functions bypasses rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

